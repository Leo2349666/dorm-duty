# ============================================================
# 数据同步配置（可选）
# ------------------------------------------------------------
# 留空（也就是把这个文件原样保留、什么都不改）= 纯本地模式：
#   数据只存在浏览器 localStorage 里，换设备/清缓存就没了，
#   需要靠「设置 → 导出 / 导入 JSON」手动搬家。
#
# 填上下面两个值 = 云端模式：多台设备输入相同的
#   「公寓 + 楼栋 + 房间号」即可实时同步。
#
# 怎么拿到这两个值见 README.md 的「Supabase 配置」章节。
# ============================================================

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
